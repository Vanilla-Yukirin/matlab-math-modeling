clc;clear;
% 计算斐波那契数列第N项模p的值
% 目标
N=100000000;
p=998244353;

% 递推关系：F(i) = F(i-1) + F(i-2)
tic
A1 = 1; % F(1)，即前一项 -> 在迭代过程中代表F(i-1)
A2 = 1; % F(2)，即当前项 -> 在迭代过程中代表F(i)
for i=3:N % 然后这里只需要从第三项开始迭代，每次迭代生成第i项
    t=A2; % 临时存一下A2，其会变成下一次的A1
    A2=mod(A1+A2,p); % 递推出
    A1=mod(t,p);
end
result=A2; % F(N)
disp(result);
toc