% 3行4列的随机矩阵
A = rand(3, 4)

% 3行10列的矩阵，元素为1到30的连续整数
B = reshape(1:30, 3, 10)

% 下三角部分为[0,1]上是均匀随机数，其余为全1的5行5列矩阵
C = ones(5) + tril(rand(5)-1)

% 计算两个向量的点积
x = [1 2 3]; y = [4 5 6];
sum(x .* y)

% 负数变0，正数不变
A = [3, -1, 4, -2, 5];
A(A<0) = 0